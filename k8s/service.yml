apiVersion: v1
kind: Service
metadata:
  name: Jordan
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "Jordan"
    port: 8080
  selector:
    app: Jordan
